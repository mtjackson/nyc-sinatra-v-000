<!DOCTYPE html>
<html>
<head>
  <title>Edit Figure</title>
</head>
<body>

  <form action="/figures/<%= @figure.id %>" method="post">
    <input type="hidden" name="_method" value="patch">
    <label>Name</label><input type="text" name="figure[name]" id="figure_name" value="<%=@figure.name%>"></p>

    <label>Edit Titles</label>
    <br></br>
    <%@titles.each do |title|%>
    <label><%=title.name%></label>
    <input type="checkbox" name="figure[title_ids][]" id="<%=title.id %>" value="<%=title.id%>" <%='checked' if @figure.titles.include?(title) %>></p>
    <%end%>
    <br></br>

    <label>Create New Title: </label><input type="text" name="title[name]" id="new_title"></p>
    <br></br>

    <label>Landmarks</label>
    <%@landmarks.each do |landmark|%>
    <label><%=landmark.name%></label>
    <input type="checkbox" name="figure[landmark_ids][]" id="<%=landmark.id %>" value="<%=landmark.id%>" <%='checked' if @figure.landmarks.include?(landmark) %>> </p>
    <%end%>
    <br></br>

    <label>Create New Landmark</label>
    <p>Name: <input type="text" name="landmark[name]" id="new_landmark">
    Year: <input type="number" name="landmark[year_completed]" id="Year Completed"></p>
    <br></br>

    <input type="submit" value="Edit Figure">
  </form>
</body>
</html>
