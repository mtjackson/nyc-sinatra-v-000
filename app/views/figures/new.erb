<!DOCTYPE html>
<html>
<head>
  <title>New Figure</title>
</head>
<body>

  <form action="/figures" method="post">
    <label>Name</label><input type="text" name="figure[name]" id="figure_name"></p>

    <label>Titles</label>
    <br></br>
    <%@titles.each do |title|%>
    <label><%=title.name%></label>
    <input type="checkbox" name="figure[title_ids][]" id="title_<%= title.id %>" value="<%=title.id%>"></p>
    <%end%>
    <br></br>

    <label>Create New Title: </label><input type="text" name="title[name]" id="new_title"></p>
    <br></br>

    <label>Landmarks</label>
    <%@landmarks.each do |landmark|%>
    <label><%=landmark.name%></label>
    <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%=landmark.id %>" value="<%=landmark.id%>"></p>
    <%end%>
    <br></br>

    <label>Create New Landmark</label>
    <p>Name: <input type="text" name="landmark[name]" id="new_landmark">
    Year: <input type="number" name="landmark[year_completed]" id="Year Completed"></p>
    <br></br>

    <input type="submit" value="Create New Figure">
  </form>
</body>
</html>
